<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:id="@+id/menuGroupInfo">

        <item
            android:id="@+id/search"
            android:title="@string/delete_icons"
            android:icon="@drawable/ic_search_large"
            app:showAsAction="always"
            android:checked="true"
            android:orderInCategory="1"
            />
   <!--     <item
            android:id="@+id/grid_size"
            android:title="Grid size"
            android:icon="@drawable/ic_grid_size"
            android:checked="true"
            app:showAsAction="always"
            android:orderInCategory="1"
            />

-->
        <!--TODO Uncomment and solve the problemO-->

        <item
            android:id="@+id/delete_icons"
            android:title="@string/delete_icons"
            android:icon="@drawable/ic_delete_board"
            app:showAsAction="ifRoom"
            android:checked="true"
            android:orderInCategory="1"
            />


    </group>

</menu>